# 📊 Diagrama: parse_arguments()

## 🔄 Fluxo Principal (Flowchart)

```
┌──────────────────────────────────┐
│  parse_arguments(argc, argv)     │
│                                  │
│  Input: argc=4, argv=["prog",   │
│                        "3",      │
│                        "2",      │
│                        "1"]      │
└────────────┬─────────────────────┘
             │
             ▼
      ┌─────────────┐
      │ stack_a =   │
      │    NULL     │
      └──────┬──────┘
             │
             ▼
┌────────────────────────────────────┐
│     LOOP: i = 1 até argc-1         │
│  (processar cada argumento)        │
└────────────┬───────────────────────┘
             │
             ▼
      ┌──────────────────────┐
      │  is_valid_input(     │
      │    argv[i])?         │ [VALIDAÇÃO]
      └───────┬─────────┬────┘
          Não │         │ Sim
              │         │
       ┌──────▼──────┐  │
       │ error_exit( │  │
       │  &stack_a)  │  │
       │ return NULL │  │
       └─────────────┘  │
                        │
                        ▼
                 ┌────────────────┐
                 │  safe_atoi(    │
                 │   argv[i],     │ [CONVERSÃO]
                 │   &num)?       │
                 └───┬────────┬───┘
                 Não │        │ Sim
                     │        │
              ┌──────▼─────┐  │
              │error_exit( │  │
              │ &stack_a)  │  │
              │return NULL │  │
              └────────────┘  │
                              │
                              ▼
                       ┌──────────────┐
                       │ node =       │
                       │ stack_new(   │ [CRIAR NÓ]
                       │   num)       │
                       └──────┬───────┘
                              │
                              ▼
                       ┌──────────────┐
                       │stack_add_    │
                       │ back(        │ [ADICIONAR]
                       │ &stack_a,    │
                       │  node)       │
                       └──────┬───────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   i++           │
                    │   Próximo arg?  │
                    └────┬──────┬─────┘
                      Sim│      │Não
                         │      │
              ┌──────────┘      │
              │                 │
              └─► (volta loop)  │
                                │
                                ▼
                    ┌──────────────────┐
                    │ has_duplicates(  │ [CHECAR]
                    │   stack_a)?      │ [DUPLICATAS]
                    └────┬────────┬────┘
                     Sim │        │ Não
                         │        │
                  ┌──────▼─────┐  │
                  │error_exit( │  │
                  │ &stack_a)  │  │
                  │return NULL │  │
                  └────────────┘  │
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ normalize_      │ [NORMALIZAR]
                         │  values(        │ [ÍNDICES]
                         │   stack_a)      │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │  return         │
                         │   stack_a       │ [SUCESSO!]
                         └─────────────────┘
```

---

## 🎯 Exemplo Concreto: `./push_swap 3 2 1`

### **Estado por Etapa**:

```
ENTRADA:
argc = 4
argv = ["./push_swap", "3", "2", "1"]
       └─ argv[0]     └─ argv[1]
                           argv[2]
                           argv[3]

═══════════════════════════════════════════════════════

ETAPA 1: Inicialização
┌────────────┐
│ stack_a =  │ → NULL
└────────────┘

═══════════════════════════════════════════════════════

ITERAÇÃO 1: Processar argv[1] = "3"
────────────────────────────────────

1. Validação: is_valid_input("3") → 1 (OK) ✓

2. Conversão: safe_atoi("3", &num) → num = 3 ✓

3. Criar nó:
   ┌─────────────┐
   │ value: 3    │
   │ index: -1   │ (ainda não normalizado)
   │ next: NULL  │
   └─────────────┘

4. Adicionar na pilha:
   stack_a
      ↓
   ┌─────────────┐
   │ value: 3    │
   │ index: -1   │
   │ next: NULL  │
   └─────────────┘

═══════════════════════════════════════════════════════

ITERAÇÃO 2: Processar argv[2] = "2"
────────────────────────────────────

1. Validação: is_valid_input("2") → 1 (OK) ✓

2. Conversão: safe_atoi("2", &num) → num = 2 ✓

3. Criar nó:
   ┌─────────────┐
   │ value: 2    │
   │ index: -1   │
   │ next: NULL  │
   └─────────────┘

4. Adicionar na pilha (ao fim):
   stack_a
      ↓
   ┌─────────────┐      ┌─────────────┐
   │ value: 3    │ ───► │ value: 2    │
   │ index: -1   │      │ index: -1   │
   │ next: ────┐ │      │ next: NULL  │
   └───────────┼─┘      └─────────────┘
               └────────────►

═══════════════════════════════════════════════════════

ITERAÇÃO 3: Processar argv[3] = "1"
────────────────────────────────────

1. Validação: is_valid_input("1") → 1 (OK) ✓

2. Conversão: safe_atoi("1", &num) → num = 1 ✓

3. Criar nó:
   ┌─────────────┐
   │ value: 1    │
   │ index: -1   │
   │ next: NULL  │
   └─────────────┘

4. Adicionar na pilha (ao fim):
   stack_a
      ↓
   ┌─────────┐    ┌─────────┐    ┌─────────┐
   │value: 3 │───►│value: 2 │───►│value: 1 │
   │index: -1│    │index: -1│    │index: -1│
   │next: ──┐│    │next: ──┐│    │next:NULL│
   └────────┼┘    └────────┼┘    └─────────┘
            └──────────────┘

═══════════════════════════════════════════════════════

ETAPA 2: Checar Duplicatas
────────────────────────────────────

has_duplicates(stack_a):
   Compara 3 com [2, 1] → diferentes ✓
   Compara 2 com [1]    → diferentes ✓
   Compara 1 com []     → fim
   
Resultado: 0 (FALSE) → Sem duplicatas ✓

═══════════════════════════════════════════════════════

ETAPA 3: Normalizar Valores
────────────────────────────────────

normalize_values(stack_a):

Valores ordenados: [1, 2, 3]
                    ↓  ↓  ↓
Índices:           [0, 1, 2]

Atribuição:
   value 1 → index 0
   value 2 → index 1
   value 3 → index 2

Pilha após normalização:
   stack_a
      ↓
   ┌─────────┐    ┌─────────┐    ┌─────────┐
   │value: 3 │───►│value: 2 │───►│value: 1 │
   │index: 2 │    │index: 1 │    │index: 0 │
   │next: ──┐│    │next: ──┐│    │next:NULL│
   └────────┼┘    └────────┼┘    └─────────┘
            └──────────────┘

═══════════════════════════════════════════════════════

RESULTADO FINAL:
return stack_a;  // Pilha pronta para ordenação!
```

---

## 🔍 Diagrama: has_duplicates()

```
┌──────────────────────────┐
│ has_duplicates(stack)    │
└──────────┬───────────────┘
           │
           ▼
    ┌──────────────┐
    │ current =    │
    │   stack      │
    └──────┬───────┘
           │
           ▼
    ┌─────────────────┐
    │ current != NULL?│ [LOOP EXTERNO]
    └─────┬──────┬────┘
      Não │      │ Sim
          │      ▼
          │  ┌─────────────┐
          │  │ compare =   │
          │  │ current->   │
          │  │   next      │
          │  └──────┬──────┘
          │         │
          │         ▼
          │  ┌─────────────────┐
          │  │compare != NULL? │ [LOOP INTERNO]
          │  └─────┬──────┬────┘
          │    Não │      │ Sim
          │        │      ▼
          │        │  ┌────────────────┐
          │        │  │current->value  │
          │        │  │    ==          │
          │        │  │compare->value? │
          │        │  └────┬──────┬────┘
          │        │   Não │      │ Sim
          │        │       │      ▼
          │        │       │  ┌─────────────┐
          │        │       │  │ return 1    │
          │        │       │  │(duplicata!) │
          │        │       │  └─────────────┘
          │        │       │
          │        │       ▼
          │        │  ┌─────────────┐
          │        │  │ compare =   │
          │        │  │ compare->   │
          │        │  │   next      │
          │        │  └──────┬──────┘
          │        │         │
          │        │         └──┐
          │        │            │ (volta loop interno)
          │        └────────────┘
          │        
          │  ┌─────────────┐
          │  │ current =   │
          │  │ current->   │
          │  │   next      │
          │  └──────┬──────┘
          │         │
          │         └──┐
          │            │ (volta loop externo)
          ▼            ▼
    ┌─────────────┐
    │  return 0   │
    │(sem dupl.)  │
    └─────────────┘

EXEMPLO VISUAL:
Pilha: [3] → [2] → [1]

Iteração 1: current=3
  compare=2: 3≠2 ✓
  compare=1: 3≠1 ✓
  
Iteração 2: current=2
  compare=1: 2≠1 ✓
  
Iteração 3: current=1
  compare=NULL (fim)
  
Resultado: 0 (sem duplicatas)
```

---

## 🔢 Diagrama: normalize_values()

```
┌──────────────────────────┐
│ normalize_values(stack)  │
└──────────┬───────────────┘
           │
           ▼
    ┌──────────────┐
    │ size =       │
    │ stack_size() │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ index = 0    │
    └──────┬───────┘
           │
           ▼
    ┌─────────────────┐
    │ index < size?   │ [LOOP: 0 até N-1]
    └─────┬──────┬────┘
      Não │      │ Sim
          │      ▼
          │  ┌──────────────────┐
          │  │ Encontrar menor  │
          │  │ valor sem índice │
          │  └────────┬─────────┘
          │           │
          │           ▼
          │  ┌──────────────────┐
          │  │ min_node =       │
          │  │   encontrado     │
          │  └────────┬─────────┘
          │           │
          │           ▼
          │  ┌──────────────────┐
          │  │ min_node->index  │
          │  │    = index       │
          │  └────────┬─────────┘
          │           │
          │           ▼
          │  ┌──────────────────┐
          │  │    index++       │
          │  └────────┬─────────┘
          │           │
          │           └──┐
          │              │ (volta loop)
          ▼              ▼
    ┌──────────────┐
    │   return     │
    └──────────────┘

EXEMPLO VISUAL:
Pilha inicial: [3,-1] → [2,-1] → [1,-1]
              valor,index

Passo 1: index=0
  Menor valor sem índice: 1
  [3,-1] → [2,-1] → [1,0] ✓

Passo 2: index=1
  Menor valor sem índice: 2
  [3,-1] → [2,1] → [1,0] ✓

Passo 3: index=2
  Menor valor sem índice: 3
  [3,2] → [2,1] → [1,0] ✓

Resultado: [3,2] → [2,1] → [1,0]
```

---

## 📊 Diagrama de Dados (Transformação)

```
┌─────────────────────────────────────────────┐
│            ENTRADA (argv)                   │
│  ["./push_swap", "3", "2", "1"]            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼ parse_arguments()
┌─────────────────────────────────────────────┐
│        PILHA A (sem normalizar)             │
│                                             │
│  HEAD → [value:3] → [value:2] → [value:1]  │
│         [index:-1]  [index:-1]  [index:-1] │
└──────────────────┬──────────────────────────┘
                   │
                   ▼ normalize_values()
┌─────────────────────────────────────────────┐
│      PILHA A (normalizada - pronta!)        │
│                                             │
│  HEAD → [value:3] → [value:2] → [value:1]  │
│         [index:2]   [index:1]   [index:0]  │
└─────────────────────────────────────────────┘
                   │
                   ▼ (passa para sort)
```

---

## ⚠️ Diagrama: Tratamento de Erros

```
ERRO: Argumento inválido (ex: "abc")
┌──────────────┐
│ Loop i=1     │
│ argv[1]="3"  │ → OK
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Loop i=2     │
│argv[2]="abc" │ → ERRO!
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ is_valid_input() │
│   retorna 0      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  error_exit(     │
│   &stack_a)      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐    Stack A antes do erro:
│ stack_clear()    │    [3] → NULL
│   libera nó 3    │         ↓
└──────┬───────────┘    (será liberado)
       │
       ▼
┌──────────────────┐
│  return NULL     │ → Sinaliza erro para main
└──────────────────┘


ERRO: Duplicata (ex: "1 2 1")
Pilha criada: [1] → [2] → [1]
                ↓
         has_duplicates() = 1
                ↓
         error_exit(&stack_a)
                ↓
         stack_clear() libera TUDO
                ↓
         return NULL
```

---

## 🧪 Casos de Teste (Visual)

```
TEST 1: Sucesso básico
Input:  ./push_swap 3 2 1
Result: [3,2]→[2,1]→[1,0] ✓

TEST 2: Duplicata
Input:  ./push_swap 1 2 1
Result: NULL (erro) ✓

TEST 3: Inválido
Input:  ./push_swap abc
Result: NULL (erro) ✓

TEST 4: Overflow
Input:  ./push_swap 2147483648
Result: NULL (erro) ✓

TEST 5: Um elemento
Input:  ./push_swap 42
Result: [42,0] ✓

TEST 6: Já ordenado
Input:  ./push_swap 1 2 3
Result: [1,0]→[2,1]→[3,2] ✓
```

---

**Este diagrama completo mostra todo o fluxo de `parse_arguments`! 🎉**
